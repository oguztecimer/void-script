---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - editor-ui/src/codemirror/voidscript-theme.ts
  - editor-ui/src/App.tsx
  - editor-ui/src/components/Header.tsx
  - editor-ui/src/components/Editor.tsx
  - editor-ui/src/components/Console.tsx
  - editor-ui/src/components/DebugPanel.tsx
  - editor-ui/src/components/ScriptList.tsx
  - editor-ui/src/components/StatusBar.tsx
  - editor-ui/src/components/TabBar.tsx
  - editor-ui/src/components/ToolStrip.tsx
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03

must_haves:
  truths:
    - "No hardcoded hex color values remain in any component file or voidscript-theme.ts (except index.html anti-flash guard)"
    - "All color, dimension, and font references use var(--token-name) from tokens.css"
    - "Inter Variable renders as the UI font at 13px across all interface elements"
    - "JetBrains Mono Variable renders in the CodeMirror editor and console without system fallback"
    - "Visual appearance is unchanged -- token migration is invisible to the user"
  artifacts:
    - path: "editor-ui/src/codemirror/voidscript-theme.ts"
      provides: "CodeMirror theme using CSS tokens for all colors and Fontsource font-family"
      contains: "var(--"
    - path: "editor-ui/src/App.tsx"
      provides: "Root layout using CSS tokens for colors and borders"
      contains: "var(--"
    - path: "editor-ui/src/components/Header.tsx"
      provides: "Title bar using CSS tokens (32 hex values migrated)"
      contains: "var(--"
    - path: "editor-ui/src/components/TabBar.tsx"
      provides: "Tab bar using CSS tokens"
      contains: "var(--"
  key_links:
    - from: "editor-ui/src/codemirror/voidscript-theme.ts"
      to: "editor-ui/src/theme/tokens.css"
      via: "CSS custom properties inherited from :root"
      pattern: "var\\(--"
    - from: "editor-ui/src/components/Header.tsx"
      to: "editor-ui/src/theme/tokens.css"
      via: "CSS custom properties in inline styles"
      pattern: "var\\(--"
    - from: "editor-ui/src/App.tsx"
      to: "editor-ui/src/theme/tokens.css"
      via: "CSS custom properties in inline styles"
      pattern: "var\\(--"
---

<objective>
Migrate all hardcoded hex color values and font references across the entire editor-ui codebase to use CSS custom property tokens from tokens.css, and apply Fontsource font-family names.

Purpose: Completes the token system (FOUN-03) by replacing 129 hardcoded hex values with var(--token) references, and applies the self-hosted font-family names (FOUN-01, FOUN-02) to the CodeMirror theme. After this plan, no hardcoded design values remain in component code.

Output: All 10 source files migrated to use CSS tokens. Visual output is identical to before -- this is a pure refactor.
</objective>

<execution_context>
@/Users/dakmor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dakmor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- tokens.css provides these CSS custom properties (created by Plan 01). -->
<!-- Use these token names exactly. Do NOT invent new token names unless a value is missing. -->
<!-- If a hex value appears in a component that has no corresponding token, add the token to tokens.css -->

Expected token categories (defined on :root):
  Background: --bg-app, --bg-editor, --bg-panel, --bg-toolbar, --bg-input, --bg-selection, --bg-hover, --bg-active, --bg-tab-active, --bg-tab-inactive, --bg-tooltip
  Text: --text-primary, --text-secondary, --text-tertiary, --text-disabled, --text-link
  Border: --border-default, --border-subtle, --border-strong
  Accent: --accent-blue, --accent-green, --accent-red, --accent-yellow
  Syntax: --syntax-keyword, --syntax-function, --syntax-string, --syntax-number, --syntax-comment, --syntax-operator, --syntax-constant, --syntax-bracket, --syntax-variable
  Dimensions: --height-titlebar, --height-tabbar, --height-statusbar, --width-toolstrip, --size-toolstrip-btn, --height-widget-btn
  Typography: --font-ui, --font-mono, --font-size-ui, --font-size-editor, --font-size-status, --line-height-editor
  Transition: --transition-hover
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate voidscript-theme.ts to CSS tokens and Fontsource fonts</name>
  <files>editor-ui/src/codemirror/voidscript-theme.ts</files>
  <action>
Update `editor-ui/src/codemirror/voidscript-theme.ts` to use CSS custom properties for ALL color values and Fontsource font-family names:

1. **Remove top-level color constants** (`background`, `foreground`, `selection`, `cursor`, `gutterBg`, `gutterFg`, `lineHighlight`). Replace all usages with `var(--token-name)`.

2. **Replace hardcoded colors in EditorView.theme():**
   - `backgroundColor: background` -> `backgroundColor: 'var(--bg-editor)'`
   - `color: foreground` -> `color: 'var(--text-primary)'`
   - `caretColor: cursor` -> `caretColor: 'var(--text-primary)'`
   - `borderLeftColor: cursor` -> `borderLeftColor: 'var(--text-primary)'`
   - `backgroundColor: selection` -> `backgroundColor: 'var(--bg-selection)'`
   - `backgroundColor: gutterBg` -> `backgroundColor: 'var(--bg-panel)'`
   - `color: gutterFg` -> `color: 'var(--text-tertiary)'`
   - `borderRight: '1px solid #393B40'` -> `borderRight: '1px solid var(--border-default)'`
   - `backgroundColor: '#393B40'` (activeLineGutter) -> `backgroundColor: 'var(--bg-hover)'`
   - `color: '#9DA0A8'` (activeLineGutter) -> `color: 'var(--text-secondary)'`
   - `backgroundColor: lineHighlight` -> `backgroundColor: 'var(--bg-panel)'`
   - Fold placeholder colors -> use appropriate tokens
   - Tooltip colors -> use `var(--bg-tooltip)`, `var(--border-subtle)`, `var(--text-primary)`
   - Autocomplete selected -> `var(--bg-selection)`
   - Search match -> `var(--bg-selection)`, outline with `var(--accent-blue)`

3. **Update font-family:**
   - `.cm-content` fontFamily: change from `"'JetBrains Mono', 'Fira Code', monospace"` to `'var(--font-mono)'`

   NOTE on CodeMirror and CSS variables in font-family: CodeMirror's `.cm-content` fontFamily is set via JavaScript object. CSS `var()` works in inline style strings. Use the var() reference directly: `fontFamily: 'var(--font-mono)'`. This works because the browser resolves var() at render time.

   Also add `fontVariantLigatures: 'none'` per user decision (JetBrains Mono ligatures OFF).

4. **Replace hardcoded colors in HighlightStyle.define():**
   - `color: '#00e5ff'` -> `color: 'var(--syntax-keyword)'`
   - `color: '#bcbec4'` -> `color: 'var(--syntax-variable)'`
   - `color: '#ffb347'` -> `color: 'var(--syntax-function)'`
   - `color: '#7a7e85'` -> `color: 'var(--syntax-comment)'`
   - `color: '#98c379'` -> `color: 'var(--syntax-string)'`
   - `color: '#d19a66'` -> `color: 'var(--syntax-number)'`
   - `color: '#56b6c2'` -> `color: 'var(--syntax-operator)'`
   - `color: '#c678dd'` -> `color: 'var(--syntax-constant)'`
   - `color: '#8b949e'` -> `color: 'var(--syntax-bracket)'`

**IMPORTANT:** CodeMirror's HighlightStyle.define() uses CSS-in-JS that gets compiled to stylesheet rules. CSS `var()` references work here because the resulting stylesheet rules are inserted into the DOM where :root custom properties are available.

After migration, NO hex color literal should remain in this file.
  </action>
  <verify>
    <automated>cd /Users/dakmor/Projects/Other/void-script && grep -cE '#[0-9A-Fa-f]{6}' editor-ui/src/codemirror/voidscript-theme.ts | xargs -I {} sh -c 'if [ {} -gt 0 ]; then echo "FAIL: {} hardcoded hex values remain"; exit 1; else echo "OK: No hardcoded hex values"; fi' && grep -q "var(--font-mono)" editor-ui/src/codemirror/voidscript-theme.ts && grep -q "var(--syntax-keyword)" editor-ui/src/codemirror/voidscript-theme.ts && grep -q "fontVariantLigatures" editor-ui/src/codemirror/voidscript-theme.ts && echo "All checks passed"</automated>
  </verify>
  <done>
    - Zero hardcoded hex color values in voidscript-theme.ts
    - All colors reference var(--token) CSS custom properties
    - font-family uses var(--font-mono) referencing JetBrains Mono Variable from Fontsource
    - fontVariantLigatures: 'none' applied to .cm-content
    - HighlightStyle colors use syntax tokens
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate all component files and App.tsx to CSS tokens</name>
  <files>editor-ui/src/App.tsx, editor-ui/src/components/Header.tsx, editor-ui/src/components/Editor.tsx, editor-ui/src/components/Console.tsx, editor-ui/src/components/DebugPanel.tsx, editor-ui/src/components/ScriptList.tsx, editor-ui/src/components/StatusBar.tsx, editor-ui/src/components/TabBar.tsx, editor-ui/src/components/ToolStrip.tsx</files>
  <action>
For EACH of the following files, replace every hardcoded hex color value with the corresponding CSS custom property token. Use `var(--token-name)` in inline style strings.

**Mapping rules:**
- `#1E1F22` (dark background) -> `var(--bg-app)` or `var(--bg-editor)` or `var(--border-strong)` depending on context (background vs border)
- `#2B2D30` (panel background) -> `var(--bg-panel)`
- `#393B40` (hover background, borders) -> `var(--bg-hover)` for backgrounds, `var(--border-default)` for borders
- `#43454A` (subtle borders, separators) -> `var(--border-subtle)`
- `#DFE1E5` (primary text) -> `var(--text-primary)`
- `#9DA0A8` (secondary text) -> `var(--text-secondary)`
- `#6F737A` (tertiary text) -> `var(--text-tertiary)`
- `#5A5D63` (disabled text) -> `var(--text-disabled)`
- `#3574F0` (blue accent) -> `var(--accent-blue)`
- `#2E436E` (selection/active blue bg) -> `var(--bg-selection)` or `var(--bg-active)`
- `#548AF7` (link blue) -> `var(--text-link)`

**Header.tsx special colors** -- The Header component has several unique colors for Run/Debug/Stop buttons and window controls. For these component-specific values that don't map to existing tokens, add NEW tokens to tokens.css following the hybrid naming convention from CONTEXT.md decisions:
- Run button: `--run-btn-icon` (#57965C), `--run-btn-bg` (#2A3B2D), `--run-btn-hover` (#334733)
- Debug button: `--debug-btn-icon` (#6B9BFA), `--debug-btn-bg` (#2E3A4E), `--debug-btn-hover` (#3A4A5E)
- Stop button: `--stop-btn-icon` (#DB5C5C), `--stop-btn-bg` (#4E2A2A), `--stop-btn-hover` (#5E3434)
- Window controls: `--window-close` (#FF5F56), `--window-minimize` (#FFBD2E), `--window-maximize` (#27C93F)

**For each file, verify by running:** `grep -cE '#[0-9A-Fa-f]{6}' {file}` should return 0.

**Files and their expected migration scope:**
1. **App.tsx** (~9 hex values): Background, border, tab strip colors, hover states
2. **Header.tsx** (~32 hex values): Toolbar background, button colors, window controls, separators. Largest migration. Add component-scoped tokens to tokens.css for button-specific colors.
3. **TabBar.tsx** (~11 hex values): Tab backgrounds, active indicator, text colors, close button
4. **ScriptList.tsx** (~11 hex values): Panel background, list item colors, selection
5. **DebugPanel.tsx** (~15 hex values): Panel background, variable display, section headers
6. **StatusBar.tsx** (~8 hex values): Bar background, text colors, borders
7. **ToolStrip.tsx** (~7 hex values): Strip background, button colors, active states
8. **Console.tsx** (~3 hex values): Output panel, text colors
9. **Editor.tsx** (~3 hex values): Any remaining inline colors (most are in voidscript-theme.ts)

**IMPORTANT:** Do NOT change any layout, structure, or functionality. Only replace color/font string values. The visual output must be pixel-identical.

**IMPORTANT:** When replacing in inline styles (React `style` prop), use the string form: `background: 'var(--bg-panel)'`. CSS `var()` works in React inline styles as long as the value is a string.

**After ALL files are migrated**, add any new component-scoped tokens (from Header.tsx buttons) to `editor-ui/src/theme/tokens.css` in a clearly labeled section:
```css
/* Component-specific tokens */
--run-btn-icon: #57965C;
/* etc. */
```
  </action>
  <verify>
    <automated>cd /Users/dakmor/Projects/Other/void-script && total=0; for f in editor-ui/src/App.tsx editor-ui/src/components/Header.tsx editor-ui/src/components/Editor.tsx editor-ui/src/components/Console.tsx editor-ui/src/components/DebugPanel.tsx editor-ui/src/components/ScriptList.tsx editor-ui/src/components/StatusBar.tsx editor-ui/src/components/TabBar.tsx editor-ui/src/components/ToolStrip.tsx; do count=$(grep -cE '#[0-9A-Fa-f]{6}' "$f" 2>/dev/null || echo 0); if [ "$count" -gt 0 ]; then echo "FAIL: $f has $count hardcoded hex values"; total=$((total + count)); fi; done; if [ "$total" -gt 0 ]; then echo "TOTAL REMAINING: $total"; exit 1; else echo "OK: Zero hardcoded hex values across all component files"; fi</automated>
  </verify>
  <done>
    - Zero hardcoded hex color values across all 9 component/app files
    - All colors reference var(--token) CSS custom properties from tokens.css
    - Component-specific tokens (run/debug/stop buttons, window controls) added to tokens.css
    - Visual appearance is pixel-identical to pre-migration state
    - No layout, structure, or functionality changes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify font rendering and visual fidelity</name>
  <files>n/a</files>
  <action>
Human verifies the complete Phase 1 Foundation work: self-hosted Inter and JetBrains Mono fonts via Fontsource, CSS token system replacing all 129+ hardcoded hex values, and macOS rendering fixes. All design values now flow through tokens.css.

Steps to verify:
1. Run `cd editor-ui and npm run build` to confirm Vite builds successfully with fonts and tokens
2. Launch the editor (cargo run or dev server) and verify:
   - Open browser DevTools -> Computed tab on any UI text element
   - Confirm font-family resolves to "Inter Variable" (not system fallback)
   - Select the CodeMirror editor area -> confirm font-family resolves to "JetBrains Mono Variable"
   - Text weight should look crisp, no artificial bold -- compare visually to JetBrains Rider if available
3. Check for white flash: close and reopen the editor window. There should be no white flash before content loads.
4. Spot-check that the UI looks identical to before the migration (no color shifts, no missing styles)
5. In DevTools, inspect any element's style -- values should show `var(--token-name)` not hardcoded hex
  </action>
  <verify>
    <automated>cd /Users/dakmor/Projects/Other/void-script/editor-ui && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - User confirms Inter Variable renders correctly as UI font
    - User confirms JetBrains Mono Variable renders in editor and console
    - No white flash on cold launch
    - Visual appearance unchanged from pre-migration
    - User types "approved"
  </done>
</task>

</tasks>

<verification>
- `grep -rE '#[0-9A-Fa-f]{6}' editor-ui/src/ --include='*.tsx' --include='*.ts' | grep -v 'tokens.css' | grep -v 'node_modules'` returns zero results (no hex outside tokens.css)
- `npm run build` in editor-ui/ succeeds
- Font files (.woff2) appear in editor-ui/dist/assets/ after build
- tokens.css has 40+ tokens covering all design values
</verification>

<success_criteria>
1. Zero hardcoded hex values in any .tsx or .ts file outside of tokens.css and index.html
2. Inter Variable font renders at 13px in all UI text (verified in DevTools)
3. JetBrains Mono Variable renders in CodeMirror editor and console (verified in DevTools)
4. macOS font-smoothing is antialiased (text is crisp, not blurry)
5. No white flash on cold launch
6. Visual appearance is unchanged from pre-migration state
7. Vite build succeeds and bundles font assets
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
