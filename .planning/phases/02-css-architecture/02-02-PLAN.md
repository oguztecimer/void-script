---
phase: 02-css-architecture
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - editor-ui/src/components/ToolStrip.tsx
  - editor-ui/src/components/ToolStrip.module.css
  - editor-ui/src/components/ScriptList.tsx
  - editor-ui/src/components/ScriptList.module.css
  - editor-ui/src/components/DebugPanel.tsx
  - editor-ui/src/components/DebugPanel.module.css
  - editor-ui/src/components/StatusBar.tsx
  - editor-ui/src/components/StatusBar.module.css
  - editor-ui/src/components/TabBar.tsx
  - editor-ui/src/components/TabBar.module.css
  - editor-ui/src/components/Console.tsx
  - editor-ui/src/components/Console.module.css
  - editor-ui/src/App.tsx
  - editor-ui/src/App.module.css
autonomous: true
requirements: [FOUN-04, PLSH-01, PLSH-02]

must_haves:
  truths:
    - "No onMouseEnter/onMouseLeave style mutations exist in any non-titlebar component"
    - "All interactive elements transition on hover with 150ms ease"
    - "Panel borders, separators, and dividers use the correct 3-level color hierarchy"
    - "Primitives (ToolBtn, PanelHeader, StatusSegment, Separator) are consumed by components that previously had inline duplicates"
    - "All non-titlebar component styling lives in CSS Modules"
  artifacts:
    - path: "editor-ui/src/components/ToolStrip.module.css"
      provides: "CSS Module for ToolStrip with :hover states"
      contains: ":hover"
    - path: "editor-ui/src/components/ScriptList.module.css"
      provides: "CSS Module for ScriptList with list item :hover"
      contains: ":hover"
    - path: "editor-ui/src/components/DebugPanel.module.css"
      provides: "CSS Module for DebugPanel layout"
      contains: "var(--"
    - path: "editor-ui/src/components/StatusBar.module.css"
      provides: "CSS Module for StatusBar layout"
      contains: "var(--"
    - path: "editor-ui/src/components/TabBar.module.css"
      provides: "CSS Module for TabBar with tab :hover and close button :hover"
      contains: ":hover"
    - path: "editor-ui/src/components/Console.module.css"
      provides: "CSS Module for Console output area"
      contains: "var(--font-mono)"
    - path: "editor-ui/src/App.module.css"
      provides: "CSS Module for App layout shell including bottom panel"
      contains: "var(--"
  key_links:
    - from: "editor-ui/src/components/ScriptList.tsx"
      to: "editor-ui/src/primitives/PanelHeader.tsx"
      via: "import and render"
      pattern: "import.*PanelHeader.*from.*primitives"
    - from: "editor-ui/src/components/DebugPanel.tsx"
      to: "editor-ui/src/primitives/PanelHeader.tsx"
      via: "import and render"
      pattern: "import.*PanelHeader.*from.*primitives"
    - from: "editor-ui/src/components/StatusBar.tsx"
      to: "editor-ui/src/primitives/StatusSegment.tsx"
      via: "import and render"
      pattern: "import.*StatusSegment.*from.*primitives"
    - from: "editor-ui/src/App.tsx"
      to: "editor-ui/src/primitives/PanelHeader.tsx"
      via: "import and render for bottom panel header"
      pattern: "import.*PanelHeader.*from.*primitives"
    - from: "editor-ui/src/components/ToolStrip.tsx"
      to: "editor-ui/src/primitives/ToolBtn.tsx"
      via: "import and render"
      pattern: "import.*ToolBtn.*from.*primitives"
---

<objective>
Migrate all non-titlebar components from inline React styles to CSS Modules. Replace every onMouseEnter/onMouseLeave hover pattern with CSS :hover pseudo-classes. Consume the primitives created in Plan 01 (ToolBtn, PanelHeader, Separator, StatusSegment) to eliminate duplicated inline helper components. Audit and correct the 3-level border color hierarchy across all components.

Purpose: After this plan, the entire non-titlebar UI uses CSS Modules with real CSS :hover, consistent 150ms ease transitions, and the correct border hierarchy. Header.tsx is intentionally excluded per user decision (title bar hover migration deferred to Claude's discretion, possibly Phase 3).

Output: 7 CSS Module files co-located with their components, 7 migrated .tsx files consuming primitives, zero onMouseEnter/onMouseLeave in non-titlebar code.
</objective>

<execution_context>
@/Users/dakmor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dakmor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-css-architecture/02-CONTEXT.md
@.planning/phases/02-css-architecture/02-01-SUMMARY.md

@editor-ui/src/theme/tokens.css

<interfaces>
<!-- Primitives created by Plan 01 — executor should import these directly -->

From editor-ui/src/primitives/ToolBtn.tsx:
```typescript
interface ToolBtnProps {
  title: string;
  onClick?: () => void;
  disabled?: boolean;
  active?: boolean;
  size?: 'default' | 'small';    // default=36px, small=26px
  variant?: 'ghost' | 'filled';  // ghost=transparent, filled=colored bg
  bgColor?: string;
  hoverBgColor?: string;
  iconColor?: string;
  className?: string;
  children: React.ReactNode;
}
export function ToolBtn(props: ToolBtnProps): JSX.Element;
```

From editor-ui/src/primitives/PanelHeader.tsx:
```typescript
interface PanelHeaderProps {
  title: string;
  actions?: React.ReactNode;
}
export function PanelHeader(props: PanelHeaderProps): JSX.Element;
```

From editor-ui/src/primitives/Separator.tsx:
```typescript
interface SeparatorProps {
  variant?: 'line' | 'gap';
  orientation?: 'vertical' | 'horizontal';
  level?: 'default' | 'subtle' | 'strong';
  size?: number;
  gap?: number;
}
export function Separator(props: SeparatorProps): JSX.Element;
```

From editor-ui/src/primitives/StatusSegment.tsx:
```typescript
interface StatusSegmentProps {
  icon?: React.ReactNode;
  label: React.ReactNode;
  onClick?: () => void;
}
export function StatusSegment(props: StatusSegmentProps): JSX.Element;
```

Border hierarchy (3-level system per user decision):
- --border-strong (#1E1F22): Outer boundaries — panel edges against app bg, statusbar top, titlebar bottom
- --border-default (#393B40): Standard separators — panel header bottom, tab bar bottom, section dividers
- --border-subtle (#43454A): Subtle dividers — inner separators, tooltip borders

Current hover occurrences to eliminate (non-titlebar):
- ToolStrip.tsx: 2 (tool buttons) — replace with ToolBtn primitive
- ScriptList.tsx: 4 (script items + ToolWindowBtn) — replace ToolWindowBtn with ToolBtn, script items get CSS :hover
- DebugPanel.tsx: 2 (ToolWindowBtn) — replace with ToolBtn
- StatusBar.tsx: 2 (StatusSegment) — replace with StatusSegment primitive
- TabBar.tsx: 4 (tab hover + close button) — CSS :hover in module
- App.tsx: 2 (PanelHeaderBtn) — replace with ToolBtn + PanelHeader primitives
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ToolStrip, ScriptList, DebugPanel, and StatusBar to CSS Modules with primitives</name>
  <files>
    editor-ui/src/components/ToolStrip.tsx,
    editor-ui/src/components/ToolStrip.module.css,
    editor-ui/src/components/ScriptList.tsx,
    editor-ui/src/components/ScriptList.module.css,
    editor-ui/src/components/DebugPanel.tsx,
    editor-ui/src/components/DebugPanel.module.css,
    editor-ui/src/components/StatusBar.tsx,
    editor-ui/src/components/StatusBar.module.css
  </files>
  <action>
For each component, create a co-located CSS Module and migrate all inline styles into CSS classes. Replace all onMouseEnter/onMouseLeave handlers with CSS :hover rules. Import and use primitives where they replace existing inline helpers.

**ToolStrip.tsx** migration:
- Create ToolStrip.module.css with `.strip` (flex column, bg var(--bg-panel), align-items center, padding-top 6px, gap 2px)
- `.strip.left` border-right: 1px solid var(--border-strong). `.strip.right` border-left: 1px solid var(--border-strong). Width: use var(--width-toolstrip) token (currently hardcoded 36px, token says 40px — use the token value).
- Remove inline ToolStrip button and import `ToolBtn` from `../primitives/ToolBtn`. Map each item to `<ToolBtn size="default" active={isActive} onClick={...} title={...}>{item.icon}</ToolBtn>`.
- The active state (--accent-blue bg, white text) should be handled via ToolBtn's `active` prop.
- Remove all onMouseEnter/onMouseLeave handlers.

**ScriptList.tsx** migration:
- Create ScriptList.module.css with `.panel` (width 220px, bg var(--bg-panel), overflow auto, flex column, border-right 1px solid var(--border-strong))
- `.groupLabel` — padding, font-size 11px, uppercase, letter-spacing, color var(--text-tertiary), font-weight 600
- `.scriptItem` — padding 4px 12px 4px 24px, cursor pointer, font-size 13px, color var(--text-primary), border-radius 4px, margin 0 4px, display flex, align-items center, gap 6px, transition background-color var(--transition-hover)
- `.scriptItem:hover` — background-color var(--bg-hover)
- `.emptyState` — padding, color var(--text-disabled), font-size 12px, font-style italic
- Replace the inline `ToolWindowBtn` with imported `ToolBtn` from primitives (small size, with close icon SVG as children).
- Replace inline panel header div with `PanelHeader` primitive: `<PanelHeader title="Scripts" actions={<ToolBtn size="small" ...>close icon</ToolBtn>} />`.
- Remove all onMouseEnter/onMouseLeave handlers.

**DebugPanel.tsx** migration:
- Create DebugPanel.module.css with `.panel` (width 250px, bg var(--bg-panel), overflow auto, flex column, border-left 1px solid var(--border-strong))
- `.section` — padding 4px 0, border-bottom 1px solid var(--border-default)
- `.sectionTitle` — for the "Variables" header row
- `.frame` — padding 3px 12px, font-size 12px, font-family var(--font-mono)
- `.frame.active` — color var(--text-primary), bg var(--bg-selection)
- `.frame.inactive` — color var(--text-tertiary)
- `.variable` — display flex, justify-content space-between, padding 2px 12px, font-size 12px, font-family var(--font-mono)
- `.variableName` — color var(--text-primary)
- `.variableValue` — color var(--text-secondary), overflow hidden, text-overflow ellipsis, white-space nowrap, max-width 140px, margin-left 8px
- `.emptyState` — padding 8px 12px, color var(--text-disabled), font-size 12px, font-style italic
- Replace inline `ToolWindowBtn` with imported `ToolBtn` from primitives.
- Replace both section headers (Frames, Variables) with `PanelHeader` primitive. The Frames header gets the close ToolBtn as action; Variables header has no actions.
- Remove all onMouseEnter/onMouseLeave handlers.

**StatusBar.tsx** migration:
- Create StatusBar.module.css with `.bar` (display flex, align-items center, height var(--height-statusbar), bg var(--bg-panel), font-size var(--font-size-status), color var(--text-secondary), border-top 1px solid var(--border-strong), gap 0)
- `.spacer` — flex 1
- Remove the inline `StatusSegment` function entirely and import `StatusSegment` from `../primitives/StatusSegment`.
- Replace each status segment usage with the primitive. For diagnostics segments that use colored text, pass the colored span as the `label` prop.
- Remove all onMouseEnter/onMouseLeave handlers.

**Border hierarchy audit for all four components:**
- Panel outer edges against --bg-app: use var(--border-strong) (#1E1F22) — ToolStrip borders, ScriptList right border, DebugPanel left border, StatusBar top border
- Section separators inside panels: use var(--border-default) (#393B40) — panel header bottom borders, section dividers
- Any inner/subtle dividers: use var(--border-subtle) (#43454A)
  </action>
  <verify>
    <automated>cd /Users/dakmor/Projects/Other/void-script/editor-ui && npx tsc --noEmit 2>&1 | head -30 && echo "---HOVER CHECK---" && grep -n "onMouseEnter\|onMouseLeave" src/components/ToolStrip.tsx src/components/ScriptList.tsx src/components/DebugPanel.tsx src/components/StatusBar.tsx 2>&1 || echo "No hover handlers found (PASS)"</automated>
  </verify>
  <done>ToolStrip, ScriptList, DebugPanel, and StatusBar have CSS Modules. Zero onMouseEnter/onMouseLeave in these four files. PanelHeader and ToolBtn primitives are consumed. StatusSegment primitive replaces inline StatusSegment. Border hierarchy uses correct 3-level tokens. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate TabBar, Console, and App layout to CSS Modules; final hover elimination</name>
  <files>
    editor-ui/src/components/TabBar.tsx,
    editor-ui/src/components/TabBar.module.css,
    editor-ui/src/components/Console.tsx,
    editor-ui/src/components/Console.module.css,
    editor-ui/src/App.tsx,
    editor-ui/src/App.module.css
  </files>
  <action>
**TabBar.tsx** migration:
- Create TabBar.module.css with `.bar` (display flex, bg var(--bg-tab-inactive), overflow hidden, min-height 36px, align-items stretch, border-bottom 1px solid var(--border-default))
- `.tab` — display flex, align-items center, gap 6px, padding 0 14px, cursor pointer, border-bottom 2px solid transparent, color var(--text-secondary), font-size 13px, white-space nowrap, user-select none, transition background-color var(--transition-hover)
- `.tab:hover` — background-color var(--bg-tab-active)
- `.tab.active` — background-color var(--bg-tab-active), border-bottom-color var(--accent-blue), color var(--text-primary)
- `.tab.active:hover` — background-color var(--bg-tab-active) (no change on hover for active tab)
- `.modified` — color var(--text-primary), font-size 8px
- `.closeBtn` — display flex, align-items center, justify-content center, width 18px, height 18px, border-radius 4px, border none, background transparent, color var(--text-tertiary), cursor pointer, font-size 14px, line-height 1, margin-left 2px, padding 0, transition background-color var(--transition-hover) and color var(--transition-hover)
- `.closeBtn:hover` — background-color var(--bg-hover), color var(--text-primary)
- Migrate the component to use className with CSS module classes. Conditionally apply `.active` class on the active tab.
- Remove all onMouseEnter/onMouseLeave handlers (both tab-level and close button).

**Console.tsx** migration:
- Create Console.module.css with `.console` (flex 1, overflow auto, padding 6px 12px, font-size 12px, line-height 1.6, font-family var(--font-mono), background-color var(--bg-editor))
- `.entry` — white-space pre-wrap, word-break break-all
- `.entry.error` — color var(--accent-red)
- `.entry.warn` — color var(--accent-yellow)
- `.entry.info` — color var(--text-primary)
- `.emptyState` — color var(--text-disabled), font-style italic, padding 8px 0
- Console has no hover handlers, but it still needs CSS Module migration from inline styles.
- Use a data attribute or conditional class for entry level: `className={styles[entry.level] || styles.info}` or compose `${styles.entry} ${styles[entry.level]}`.

**App.tsx** migration:
- Create App.module.css with layout classes:
- `.app` — display flex, flex-direction column, height 100vh, background var(--bg-app)
- `.main` — display flex, flex 1, overflow hidden
- `.center` — display flex, flex-direction column, flex 1, overflow hidden
- `.editorArea` — flex 1, overflow hidden
- `.bottomPanel` — height 200px, display flex, flex-direction column, border-top 1px solid var(--border-strong)
- Remove the inline `PanelHeaderBtn` function entirely — replace with imported `ToolBtn` from primitives (small size).
- Replace the inline bottom panel header div with `PanelHeader` primitive, passing clear and close ToolBtn instances as actions.
- Replace the inline `BottomTab` function with a CSS Module class: `.bottomTab` — padding 4px 12px, font-size 12px, cursor pointer, user-select none, border-bottom 2px solid transparent, color var(--text-tertiary). `.bottomTab.active` — color var(--text-primary), border-bottom-color var(--accent-blue). Keep BottomTab as a local component but with CSS Module classes instead of inline style.
- Remove all onMouseEnter/onMouseLeave handlers.

**Border hierarchy audit for these three components:**
- Tab bar bottom border: var(--border-default) (separator between tabs and editor)
- Bottom panel top border: var(--border-strong) (panel edge against center area)
- Bottom panel header bottom border: var(--border-default) (section separator)

**Final verification sweep:** After completing all migrations, do a project-wide grep for onMouseEnter/onMouseLeave. The ONLY remaining occurrences should be in Header.tsx (title bar exception per user decision). If any others are found, fix them.
  </action>
  <verify>
    <automated>cd /Users/dakmor/Projects/Other/void-script/editor-ui && npx tsc --noEmit 2>&1 | head -30 && echo "---HOVER CHECK---" && grep -rn "onMouseEnter\|onMouseLeave" src/ --include="*.tsx" 2>&1 | grep -v "Header.tsx" || echo "Only Header.tsx has hover handlers (PASS)" && echo "---BUILD CHECK---" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>TabBar, Console, and App have CSS Modules. Zero onMouseEnter/onMouseLeave outside Header.tsx. Bottom panel uses PanelHeader primitive. BottomTab uses CSS Module classes. All inline styles migrated to CSS Modules. Vite build succeeds. Border hierarchy correct across all components.</done>
</task>

</tasks>

<verification>
1. `grep -rn "onMouseEnter\|onMouseLeave" editor-ui/src/ --include="*.tsx" | grep -v Header.tsx` returns empty — no hover handlers outside title bar
2. `ls editor-ui/src/components/*.module.css` shows 6 CSS Module files (ToolStrip, ScriptList, DebugPanel, StatusBar, TabBar, Console)
3. `ls editor-ui/src/App.module.css` exists
4. `grep -rn "import.*from.*primitives" editor-ui/src/` shows primitives consumed by ToolStrip, ScriptList, DebugPanel, StatusBar, App
5. `grep -rn "transition" editor-ui/src/components/*.module.css editor-ui/src/primitives/*.module.css` shows var(--transition-hover) in all interactive CSS Modules
6. `grep -rn "border-strong\|border-default\|border-subtle" editor-ui/src/components/*.module.css editor-ui/src/App.module.css` confirms correct 3-level usage
7. `npm run build` succeeds
</verification>

<success_criteria>
- Zero onMouseEnter/onMouseLeave in non-titlebar components (FOUN-04)
- All hover transitions use 150ms ease via var(--transition-hover) (PLSH-01)
- 3-level border hierarchy correctly applied: --border-strong for panel edges, --border-default for separators, --border-subtle for inner dividers (PLSH-02)
- Primitives (ToolBtn, PanelHeader, StatusSegment) consumed by at least 4 components
- CSS Modules co-located with all 7 migrated components + App
- Vite production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-css-architecture/02-02-SUMMARY.md`
</output>
